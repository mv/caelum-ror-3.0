<!DOCTYPE html>
<html>
<head>
  <title>Votaprato</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>

<h5>Controle de Restaurantes™</h5>

<body>

  <p style="color: green"><%= flash[:notice] %></p>

  <% flash.each do |key,msg| %>
    <%= content_tag :p, msg, :class => "flash-#{key}"%>
  <% end %>

  <%= yield %>

</body>
</html>
